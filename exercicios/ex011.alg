algoritmo "ex011"
var
   a, b, c, delta: real
inicio
   escreva("a: ")
   leia(a)

   escreva("b: ")
   leia(b)

   escreva("c: ")
   leia(c)

   delta <- b^2 - 4 * a * c
   escreval("delta: ", delta)
fimalgoritmo